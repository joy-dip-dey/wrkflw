version: 1.0

description: A basic workflow.

input:
  - nam

tasks:
  task1:
    input: 
      nam:  'Joydip'
    action: workflow_pack.getname
    next:
      - when: <% succeeded() %>
        publish:
          - stdout: <% result().stdout %>
          - stderr: <% result().stderr %>

output:
  - stdout: <% result().stdout %>