version: 1.0

description: A basic workflow.

input:
  - nam

vars:
  - result: ''

tasks:
  task1:
    input: 
      nam:  <% ctx(nam) %>
    action: workflow_pack.getname
    next:
      - when: <% succeeded() %>
        publish:
          - stdout: <% result().stdout %>
          - stderr: <% result().stderr %>

output:
  - result: <% ctx(result) %>